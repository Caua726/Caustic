use "std/mem.cst";
use "std/linux.cst";
use "std/string.cst";

fn main() as i32 {
    gheapinit(1024);
    let is String as s with mut = String("Hello, World!");
    write(STDOUT, s.ptr, cast(i64, s.len));
    write(STDOUT, "\n", 1);
    s = String("Olá Mundo!");
    write(STDOUT, s.ptr, cast(i64, s.len));
    write(STDOUT, "\n", 1);
    string_free(s);
    return 0;
}