// Imported FIRST by the entry file, and narrows the hub to just `leafa`.
// Its filter must not follow the hub into wide.cst below.
use "shared.cst" only leafa as shared;

fn narrow_sum() as i64 { return shared.leafa.a_val(); }
