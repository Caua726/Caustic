use "leaf.cst" as leaf;

// One-level alias of a struct owned by another module.
type Vec2 = leaf.Vec2;
// Alias whose element type lives in another module: the stride has to come out
// as the element's real size, not zero.
type Vec2x4 = [4]leaf.Vec2;
