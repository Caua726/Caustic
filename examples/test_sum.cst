fn main() as i32 {
    let is i64 as sum with mut = 0;
    let is i64 as i with mut = 0;
    while (i < 4) {
        sum = sum + i * 10 + 10;
        i = i + 1;
    }
    syscall(60, sum);
    return 0;
}
